# 완료 기준 (DoD) 현재 상태

`npm test` 로 확인되는 것과, 실기기에서 사람이 직접 봐야 하는 것을 나눠 적는다.
**"코드를 썼다"는 통과가 아니다.** 확인 방법이 없는 항목은 미완으로 둔다.

## 보안 코어 — 통과

- [x] UI 없이 단위 테스트만으로 암·복호화, 래핑/언랩, PIN 검증, 복구 코드 언랩이 모두 통과
      → `tests/crypto.test.ts`, `tests/vault.test.ts`
- [x] 같은 nonce 가 두 번 생성되지 않음을 테스트로 확인
      → `tests/crypto.test.ts` "nonce 는 두 번 생성되지 않는다" (5만 개 생성, 전부 다름)
      → 난수원이 고장 나면 조용히 넘어가지 않고 예외를 던지는 것까지 확인

## MVP

- [x] **실기기에서 잠금 → 등록 → 검색 → 보기 → 복사 → 수정 → 자동 잠금 전 구간 동작**
      → 안드로이드 실기기에서 **전 구간을 확인했다** — 금고 만들기 · 잠금 해제 · 등록 ·
        검색 · 보기 · 복사 · **수정** · 자동 잠금.
        화면이 스크롤 없이 한 화면에 들어오는 것도 확인했다.

      실기기에서만 드러난 것들 (모두 고쳐서 올림):
      - 최초 설정 중 자동 잠금이 걸려 복구 코드가 눈앞에서 사라졌다.
        가만히 있어야 하는 화면에서 가만히 있었다고 잠근 셈이다.
        → 설정 화면에서는 잠그지 않는다 (`src/app/lockPolicy.ts`, 테스트 있음)
      - 잠금을 푼 뒤의 오류를 아무도 받지 않는 자리에 두어서, 금고는 열렸는데
        화면이 잠금에 멈추고 아무 문구도 뜨지 않았다. PIN 도 지문도 똑같이 먹통.
        → 금고가 열리면 화면부터 옮기고, 그 뒤 실패는 문구로 알린다
      - 항목 하나가 손상되면 금고 전체가 안 열렸다 → 열리는 것은 다 보여 주고
        "N개 항목을 열지 못했습니다"로 알린다
      - 알림 상자가 화면 아래에서 버튼·내비게이션 바에 가려 잘렸다 → 위쪽으로.
        그랬더니 이번에는 위에서 시계·배터리를 가렸다. 위치를 정하는 곳이 두 군데라
        (안전 영역 여백 + 상자의 top) 기기마다 결과가 달랐다 → 상자를 안전 영역 밖에
        두고 상자가 스스로 한 번만 계산하게 했다 (`src/app/components/Toast.tsx`).
        **고친 뒤 실기기에서 확인했다.**
      - 잠금 화면이 길어 실패 안내가 밀려났다 → 로고를 빼고 한 화면에 담음
      - "지문·얼굴으로도" 조사 오류, 복구 코드 줄바꿈, 스위치 글자(끔→꺼짐)

      여기서 돌려 볼 수 있는 것(테스트·타입·번들)은 다 통과했는데도 이만큼 나왔다.
      실기기 확인을 건너뛰면 안 되는 이유다.
- [x] 앱 데이터 파일을 직접 열었을 때 평문 비밀번호가 0건
      → `tests/records.test.ts` "저장 파일에는 평문이 0건이다". 저장소에 실제로 들어가는
        직렬화 결과를 그대로 훑어서 서비스명·아이디·비밀번호·메모·PIN 을 모두 찾는다.
        SQLite 어댑터도 같은 `VaultRecord` 를 그대로 넣으므로 기기에서도 같은 값이 저장된다.
- [x] 앱 전환 화면·최근 앱 목록에 금고 내용이 보이지 않음
      → **안드로이드 실기기에서 확인했다.** 최근 앱 목록에 금고 내용이 보이지 않고,
        화면 찍기도 막힌다. 안드로이드는 `expo-screen-capture` 로 FLAG_SECURE,
        iOS 는 `PrivacyShield` 가름 뷰(iOS 는 아직 확인 못 함).

        여기까지 오는 데 한 번 뚫렸다. 최근 앱 목록에 저장해 둔 비밀번호가 그대로 보였다.

        원인 1: 라이브러리가 이름표를 JS 쪽에 먼저 등록하고 네이티브를 나중에 부른다.
        네이티브가 실패해도 이름표는 남아서 같은 이름표로는 다시 시도되지 않는다.
        앱이 막 뜨는 순간에는 안드로이드 화면이 아직 붙기 전이라 실패할 수 있는데,
        하필 그때가 처음 거는 시점이었다.
        원인 2: 실패를 빈 catch 로 삼켜서, 안 걸렸다는 사실 자체를 알 수 없었다.

        고친 뒤(`src/app/platform/screenGuard.ts`): 걸기 전에 이름표를 떼고 다시 붙인다.
        앱이 뜰 때 세 번까지, 다시 앞으로 나올 때마다 한 번 더 건다. 실패하면 화면에
        이유와 함께 알린다.
- [x] 클립보드가 설정 시간 후 실제로 비워짐
      → **안드로이드 실기기에서 양쪽 다 확인했다.** 60초 안에는 카카오톡에 붙고,
        60초를 넘기면 지워져서 안 붙는다.

        여기까지 오는 데 한 번 틀렸다. 앱을 벗어날 때 비우게 해 두어서 카카오톡에
        붙여넣기가 아예 안 됐다. 복사는 원래 다른 앱에 붙이려고 하는 것인데, 앱을
        벗어나는 순간 지우면 쓸모가 없어진다. 지금은 명세대로 60초 뒤에만 비운다.
        앱이 떠 있는 동안은 타이머로, 다른 앱에 다녀오면 돌아왔을 때 확인해서 지운다
        (안드로이드는 뒤로 간 앱의 타이머를 멈춘다).
      → 앱이 못 하는 부분은 위협 모델에 적었다: 삼성 클립보드 **기록**은 앱이 못 지우고,
        안드로이드 13+ 의 민감 데이터 표시는 지금 라이브러리로는 넣을 수 없다.
- [x] PIN 연속 실패 대기 시간이 앱 재시작 후에도 유지됨
      → `tests/lockout.test.ts` "실패 횟수는 앱을 다시 켜도 남아 있다"
      → **안드로이드 실기기에서 확인했다.** 대기 시간은 5회 30초, 6회 1분, 7회 5분이고,
        5번 틀린 뒤 앱을 완전히 끄고 다시 켜도 대기 상태가 그대로 남아 있었다.
        여기서 초기화됐다면 잠금 지연 자체가 무의미해진다 — 껐다 켜기만 하면
        무한정 시도할 수 있기 때문이다.
      → 시계를 되돌리는 우회, 기록 파일만 지우는 우회도 함께 막았는지 확인한다.

## 백업/복구 — 통과

- [x] 기기 초기화(앱 삭제 후 재설치) → 백업 파일 + 백업 비밀번호로 복구 성공
      → `tests/backup.test.ts`. 저장소·키 저장소를 모두 새로 만든 뒤 되살린다.
- [x] 잘못된 백업 비밀번호로는 복구 실패하며 앱이 죽지 않음
      → 틀린 비밀번호·빈 값·손댄 파일·형식이 다른 파일 모두 사용자 문장으로 끝난다.
- [x] PIN 을 잊은 상태에서 복구 코드만으로 금고 열기 성공
      → `tests/vault.test.ts` "PIN 을 잊어도 복구 코드로 열린다"

## 출시 전

- [x] 로그·크래시 리포트에 민감 정보 없음
      → `tests/hygiene.test.ts` 가 소스 전체에서 `console.` 을 찾아 실패시킨다.
        릴리스 빌드는 `transform-remove-console` 로 한 번 더 지운다.
        크래시 리포트 SDK 자체를 넣지 않았다.
- [x] **본문 16sp — 명세 3장(최소 17sp)을 벗어난 결정. 기기에서 확인하고 16 으로 확정했다 (2026-09-02)**
      → 디자인 시안이 본문 16 이고, 사용자가 16 으로 정했다. 이 숫자는 원래 중장년층이
        주 대상이라 정한 것이라 **실기기에서 읽히는지 보고 나서** 확정했다. 읽힌다.
      → 되돌릴 곳은 `src/app/theme/index.ts` 의 `font.body` 한 줄이다.
        나중에 실제 사용자에게서 "작다" 는 말이 나오면 여기만 고치면 된다.
      → 함께 작아진 것: 제목 26→20, 버튼 22→18, 숫자판 34→28, 설명 13.
        설명 13 은 이 앱에서 가장 작은 글자다. 여기가 제일 먼저 문제가 될 곳이다.
- [x] **글꼴 Pretendard 를 앱에 넣었다 — 기기에서 확인했다 (2026-09-06)**
      → SIL OFL 1.1. 라이선스 원문을 직접 받아 읽고 `assets/fonts/Pretendard-OFL.txt` 에
        같이 넣었다. OFL 은 소프트웨어에 넣어 파는 것을 명시적으로 허용한다
        (글꼴 자체를 따로 팔거나, 고친 것에 같은 이름을 붙이는 것만 금지).
      → 두 벌(Regular · SemiBold)만 넣었다. 앱이 약 3MB 늘어난다.
      → 굵기는 글꼴 이름으로 고른다. 안드로이드에 굵기 짝짓기를 맡기면 없는 굵기를
        기계가 억지로 굵게 그려(가짜 굵기) 획이 뭉개진다. 되살아나지 않도록
        `tests/hygiene.test.ts` 에 검사 두 개를 붙였다.
      → **기기에서 글꼴이 실제로 바뀌었는지 확인해야 한다.** 이름을 틀리면 조용히
        시스템 기본 글꼴로 나오고, 화면만 봐서는 알기 어렵다.
- [x] **개발용 빌드로 빠르게 고치는 길 — 실기기에서 확인했다**
      → `잠김 (개발)` 이 진짜 앱과 나란히 깔리고, 피시의 화면 서버에 붙는 것까지 확인.
        이제 화면 코드를 고칠 때 클라우드 빌드를 기다리지 않는다 (`docs/빠르게-고치기.md`).
      → 개발용 빌드에만 인터넷 권한이 열린다. 배포용은 막힌 채다
        (`tests/appConfig.test.ts` 가 프로필 여덟 가지로 검사한다).
- [x] **국제화 — 화면 문장을 옮긴 뒤 화면이 그대로인 것을 실기기에서 확인**
      → 문장 197개를 `src/app/i18n/ko.ts` 로 옮겼다. 옮긴 뒤 기기에서 달라진 것이 없다.
        197개를 건드렸는데 티가 안 나는 것이 성공이다.
- [x] **글꼴이 달라도 글자가 상자 한가운데 — 실기기에서 확인했다 (2026-09-06)**
      → 일본어로 바꿨을 때 숫자판 숫자가 동그라미 안에서 아래로 처졌다. 안드로이드가
        글꼴이 정해 둔 위아래 여백을 글자 상자에 더하는데, 그 여백이 글꼴마다 다르다.
        한국어에서는 Pretendard 여백에 맞춰 **우연히** 맞아 보였을 뿐이다.
      → `theme` 의 `CENTER_GLYPH` 로 한 곳에 적고 숫자판·켬끔 단추에서 쓴다.
        크기를 정해 둔 상자에 글자를 가운데 두는 곳이면 어디든 이걸 준다.
      → **한국어와 일본어 둘 다 기기에서 확인했다 (2026-09-11).** 한국어도 봐야
        했다 — 같은 코드를 타게 되면서 오히려 틀어질 수 있었다.
- [x] **정보 화면 — 처리방침·오픈소스 라이선스·판 번호 (2026-09-09)**
      → 설정 → 정보. 경쟁 앱을 보다가 우리에게 없는 것을 발견해서 만들었다.
      → **처리방침 전문을 앱 안에 담았다.** 웹 주소 하나만 걸어 두는 앱이 많은데,
        인터넷 권한도 요청하지 않는 앱이 자기 처리방침을 보려면 인터넷이 필요하다는
        것은 앞뒤가 맞지 않는다. 스토어용 웹 주소는 그대로 따로 필요하다.
      → **오픈 소스 라이선스.** 글꼴 두 벌이 OFL 1.1 이고, 이 허락서는 저작권 표시와
        허락서를 사본마다 함께 배포할 것을 요구한다. 글꼴 파일 이름표 안에도 들어
        있지만(자를 때 지켰다) 사람이 읽을 수 있는 곳에도 둔다. 허락서 본문은
        옮기지 않는다 — 원문이 효력을 갖는다.
      → `tests/licenses.test.ts` 5개. 글꼴 파일 목록과 맞춰 보고(하나 더 넣고
        적기를 잊는 것이 실제로 있을 법하다), 손으로 옮겨 적은 저작권 표시가 원본과
        같은지 보고, 허락서가 잘리지 않았는지 본다. 셋 다 깨뜨려서 확인했다.
      → 일본어 새 한자 45자가 또 걸려서 글꼴을 다시 잘랐다 (308KB).
- [x] **연락처 이메일 — `jamgim.app@gmail.com` (2026-09-12)**
      → 앱 전용 주소를 새로 만들었다. 개인 주소를 쓰지 않는다 — 이 주소는 스토어
        목록과 개인정보처리방침에 **공개로** 실리고 수집 프로그램에도 잡힌다.
      → 두 곳에 넣었다: `src/app/licenses.ts` 의 `CONTACT_EMAIL`,
        `docs/개인정보처리방침.md` 7항.
      → `tests/licenses.test.ts` 가 **두 곳이 같은지** 본다. 앱과 문서에 다른
        주소가 적히면 심사에서 "연락이 닿지 않는다" 로 걸리고, 무엇보다 사용자가
        어디로 보내야 할지 모르게 된다. 사람이 맞춰 두는 규칙은 언젠가 어긋난다.
- [x] **자동 완성 — 다른 앱에서 우리 줄이 뜨고 채우기 화면이 열린다 (2026-09-11)**
      → 실손24 앱 로그인 화면에서 아이디 칸을 누르니 자판 위에 "잠김" 이 뜨고,
        누르니 채우기 화면이 열렸다. 어느 앱이 달라고 하는지와 웹 주소가 제대로
        나왔다 (`실손24`, `www.silson24.or.kr`).
      → 폰 설정의 "기본 서비스" 가 잠김이다. 삼성 패스·Google 은 꺼져 있다.
      → **브라우저에서는 안 된다.** 브라우저가 시스템 자동 완성을 거치지 않고
        자기 비밀번호 관리자를 쓴다. 우리 결함이 아니지만 사용자도 겪는다
        (`docs/자동완성.md` 9장).
      → 아직 고를 목록은 없다. 그것이 3-2단계다.
      → 자판 위 줄의 글씨가 너무 흐려 못 알아보셨다. 그 줄은 안드로이드가 그려서
        우리 팔레트를 못 쓴다. 색을 못박고 밝은/어두운 모드를 따로 뒀다.
        **실기기 재확인 필요.**
- [x] **정보 화면 — 실기기에서 확인했다 (2026-09-11)**
      → 설정 → 정보 → 앱 정보 보기. 판 번호, 개인정보 처리방침 전문, 오픈 소스
        라이선스가 다 보인다. 문의 줄은 이메일을 아직 안 정해서 안 나온다.
      → **처음 올린 빌드에서는 들어가는 길이 없었다.** 화면도 길 이름도 다 만들어
        놓고 설정 화면에 단추를 넣는 수정만 조용히 빠졌다. 글자를 바꿔 넣는
        스크립트가 찾는 글을 못 찾고 아무 말 없이 지나간 것이다. 타입 검사도
        테스트도 못 잡는다 — 코드는 다 맞고 아무도 그 화면을 부르지 않을 뿐이다.
        `tests/hygiene.test.ts` 에 검사를 넣었다.
- [x] **리액트 화면이 둘인데도 앱이 평소대로 뜬다 — 실기기에서 확인했다 (2026-09-11)**
      → 자동 완성 채우기 화면은 **다른 액티비티**에서 도는 두 번째 리액트 뿌리다
        (`docs/자동완성.md`). Expo 앱에서 이게 되는지가 그 일 전체에서 제일
        불확실한 자리였다. 본 화면이 멀쩡히 뜨고 금고가 평소대로 열린다.
      → 자동 완성 자체가 되는지는 아직 모른다. 이건 **평소 앱이 안 깨졌다**는
        확인이다.
- [x] **PIN 최소 6자리 — 폰을 잃어버렸을 때 남는 벽을 두껍게 했다 (2026-09-09)**
      → 예전에는 4자리를 허용했다. 4자리는 1만 가지다. 명세 5.4 의 대기(8회 실패부터
        15분)를 감안해도 전부 해 보는 데 약 넉 달이고, `1234`·`0000`·생일을 먼저
        넣어 보는 사람에게는 그 계산조차 의미가 없다. 6자리면 100만 가지라 28년이다.
      → 폰과 PIN 을 둘 다 가진 사람은 열 수 있다. 그건 잠금장치의 정의다. 하지만
        폰만 주운 사람에게 남는 벽은 이 숫자 하나뿐이라 여기서 아낄 데가 아니다.
      → **여는 쪽 최소는 4자리 그대로 둔다.** 같이 올리면 4자리로 금고를 만든 사람이
        앱을 갱신한 순간 자기 금고를 영영 못 연다. 그건 보안이 아니라 자료 유실이다.
        그 사람은 설정에서 PIN 을 바꿀 때 6자리를 받는다.
      → `tests/pinLength.test.ts` 8개. 그 시절 금고(4자리)를 만들 방법이 이제 없어서,
        금고를 만들 때와 똑같은 방식으로 4자리 자물쇠를 끼워 넣어 되살린 뒤 열리는지
        본다. 여는 쪽 최소를 6으로 올려 놓으면 그 검사가 걸린다.
      → 문장 4개 언어를 다 고쳤고, 일본어 새 한자(短·携·帯·壁)가 잘라 놓은 글꼴에
        없어서 `tools/fonts/subset_ja.py` 를 다시 돌렸다 (296KB, OFL 문구 유지).
        글꼴 검사가 이것을 잡았다.
      → **기기에서 확인했다 (2026-09-11).** 예전에 만든 금고가 그대로 열리고,
        새 PIN 은 6자리 전에는 바꾸기가 눌리지 않는다.
- [x] **메모 칸이 자판에 가리던 것 — 실기기에서 확인했다 (2026-09-11)**
      → 메모를 쓰려고 누르면 자판이 올라와 **메모 칸을 그대로 덮었다.** 무엇을 치고
        있는지 보이지 않는다.
      → 안드로이드 15부터 화면이 끝까지 차오르면서(edge-to-edge) **자판이 올라와도
        창이 줄지 않는다.** 예전에는 창이 줄어 아래 입력창이 저절로 밀려 올라갔다.
      → `KeyboardAvoidingView` 는 여기서 안 듣는다. "보이는 화면의 아래끝"으로
        계산하는데 창이 줄지 않으니 그 값이 화면 맨 아래 그대로라, 가려진 만큼을
        0 으로 본다 (`KeyboardAvoidingView.js` 의 `_relativeKeyboardHeight`).
      → 자판 높이를 직접 받아서(`keyboardDidShow` 의 `endCoordinates.height`) 화면
        전체를 그만큼 줄인다. 스크롤도 짧아지고 아래 단추도 자판 위로 올라온다.
        그리고 손이 닿은 입력창이 가려져 있으면 스크롤이 밀어 올린다 — 메모뿐 아니라
        모든 입력창에 걸린다.
      → 기기에서 확인했다. 메모를 누르면 메모 칸과 저장 단추가 함께 보인다.
- [ ] **화면 캡처 막기가 "꺼짐"인데 캡처가 안 되던 것 — 고쳤다. 실기기 확인은 아직이다**
      → 설정에서 꺼짐으로 바꾸면 캡처가 됐는데, 하루 지나 앱을 다시 켜니 **화면은
        여전히 "꺼짐" 인데 캡처가 안 됐다.** 껐다 켜야 풀렸다.
      → 앱이 켜질 때는 저장된 설정을 아직 못 읽어서 **기본값(켬)으로 먼저 건다.**
        그 다음 저장된 "꺼짐" 을 읽는데, 예전 코드는 꺼짐이면 **아무것도 하지 않고
        돌아섰다.** 걸어 둔 것을 아무도 풀지 않으니 그대로 남았다.
      → 안전한 쪽으로 틀린 것이지만 그냥 둘 수 없다. **화면이 거짓말을 하고 있었다.**
        설정을 못 믿으면 켬 표시도 못 믿는다.
      → 이제 꺼짐이면 끄기를 부른다. 켜기와 끄기는 한 줄로 세워서 늦게 시킨 것이
        늦게 도착하게 했다 — 안 그러면 어느 것이 이길지 순서에 달린다.
      → 판단을 `screenGuardPolicy.ts` 로 떼어 내 실기기 없이 확인한다
        (`tests/screenGuard.test.ts`, 10개).
      → 확인해야 할 것: 꺼짐으로 두고 하루 뒤에 열어도 캡처가 되는지.
        켬으로 두고 하루 뒤에 열면 안 되는지.
- [x] **금고 목록이 죽던 것 — 실기기에서 확인했다 (2026-09-11)**
      → `NativeDatabase.prepareAsync … NullPointerException`. 실기기에서 **세 번** 났다.
      → **앞의 진단 두 개는 틀렸다.** '두 번 열기' 도 'OnDestroy 가 닫는다' 도 아니었다.
        뒤엣것이라면 오류 문구가 "Access to closed resource" 여야 하는데 그렇지 않았다.
      → 진짜 원인은 **한 파일에 손잡이가 두 개 생기는 것**이다. `SQLiteModule.kt` 의
        생성자는 같은 경로를 다시 열면 새로 열지 않고 이미 열려 있는 것을 돌려준다.
        그리고 `SharedObjectRegistry.delete` 는 자바스크립트 손잡이가 쓰레기 수집될 때
        `sharedObjectDidRelease()` → `ref.close()` 로 **진짜 데이터베이스를 닫는다.**
        이 길은 `isClosed` 를 켜지 않아서, 남은 손잡이는 "닫혔다"는 점잖은 오류 대신
        닫힌 내부를 그대로 만져 NullPointerException 을 낸다.
      → **지난번 고침이 그 조건을 만들고 있었다.** 질의가 실패하면 손잡이를 그냥 버리고
        새로 열었는데, 버려진 손잡이가 쓰레기 수집되면서 새로 연 것을 닫았다.
        살리려던 것이 죽이고 있었다. 그래서 고친 빌드에서 또 났다.
      → 고침 세 가지.
        ① `useNewConnection` 으로 연다 — 우리 손잡이가 남의 것과 절대 겹치지 않는다.
        ② **놓을 때는 반드시 닫는다.** 그냥 버리는 길을 API 에서 없앴다 (`Handle` 에
           `reset()` 이 없다). 다시 열기도 닫은 뒤에 한다.
        ③ 앱이 뒤로 갈 때 미리 닫고 놓는다 — 돌아왔을 때 첫 화면이 비어 보이지 않게.
      → 거래(`putMany`) 한복판에서는 다시 열지 않게 했다. 연결을 갈아 끼우면 열려 있던
        거래가 통째로 날아간다.
      → 기기에서 확인했다. 뒤로 갔다 돌아오기를 되풀이해도 목록이 그대로 열린다.
        **세 번째 만에 잡았다.** 앞의 두 진단은 틀렸고, 두 번째 고침은 오히려 이
        오류를 만들고 있었다. 소스를 끝까지 읽기 전까지는 고친 것이 아니었다.
- [x] **일본어 글꼴 — 실기기에서 확인했다 (2026-09-06)**
      → 「自分だけのパスワード保管庫」「金庫を開く」「暗証番号を忘れました」에서
        한자·히라가나·가타카나가 **한 글씨체로** 보인다. 섞이지 않았다.
      → Pretendard 에 한자가 없어 시스템 글꼴이 대신 그리던 것을 고친 결과다
        (`docs/국제화.md` 5-3).
- [x] **어두운 모드 — 실기기에서 확인했다 (2026-09-02)**
      → 확인한 것 넷. ① 설정에서 "어둡게" 를 누르면 즉시 바뀐다. ② 앱을 완전히 껐다
        켰을 때 **잠금 화면부터** 어둡다. ③ 폰은 밝은 모드인데 앱만 어둡게 둔 상태가
        유지된다. ④ "폰 설정대로" 로 두면 폰을 따라간다.
      → ②가 이 작업의 핵심이다. 잠금 화면은 금고를 **열기 전에** 그려지므로, 설정이
        금고 안에만 있으면 애초에 불가능했다.
      → 설정에 "화면 밝기"(폰 설정대로 / 밝게 / 어둡게)를 두었다. 고른 값은 잠금
        화면에서도 읽혀야 해서 금고 밖에 따로 보관한다 (`src/core/prefs.ts`).
        실패 기록 파일에 얹지 않았다 — 그 파일은 명세 5.4 의 대기 시간 카운터라서,
        밝기를 바꿀 때마다 같이 다시 쓰이면 안 된다.
      → 비밀이 아니어도 평문으로 두지 않는다. 실패 기록과 똑같이 기기 키로 감싼다.
        `tests/prefs.test.ts` 가 디스크에 평문이 남지 않는 것과, 실패 기록 파일을
        이 자리에 갖다 놓아도 열리지 않는 것을 확인한다.
      → 색 두 벌의 대비를 `tests/palette.test.ts` 가 자동으로 잰다 (15쌍 × 2벌).
        일부러 나쁜 색을 넣어 검사가 실제로 잡는 것까지 확인했다.
      → 어두운 모드에서는 기본 버튼을 뒤집는다. 먹색 버튼은 어두운 바탕에 묻힌다.
- [x] **글꼴 200% 확대 상태에서 8개 화면 모두 사용 가능 — 실기기에서 확인했다 (2026-09-02)**
      → 폰 글꼴 크기를 제일 크게 두고 화면들을 넘겨 확인했다. 글자가 잘리거나
        버튼이 겹치거나 화면 밖으로 나가는 곳이 없었다.
      → 높이를 고정하지 않고 `minHeight` 만 쓴 것이 통했다. 높이를 픽셀로 박아 두면
        글자만 커지고 상자는 그대로라서 글자가 잘린다.
      → **잠금 화면 로고를 키운 뒤 다시 확인했다.** 그림 34→56, "잠김" 20→24 로 키우고도
        200% 에서 맨 아래 "PIN(핀)을 잊었어요" 까지 스크롤 없이 보인다.
        여기서 더 키운다면 이 확인을 다시 해야 한다 — 잠금 화면에 스크롤이 생기면
        "몇 번 틀렸고 얼마나 기다려야 하는지" 가 위로 밀려 안 보인다.
- [x] 앱 아이콘 · 적응형 아이콘 · 알림줄 아이콘 · 스토어 대표 그래픽
      → 먹색 바탕에 흰 자물쇠와 `잠김`. 글꼴을 쓰지 않고 도형으로 직접 그렸다
        (쓸 수 있는 한글 글꼴이 GPL 중국어 글꼴뿐이라 파는 앱 로고에 넣기 곤란했다).
        만드는 법과 크기 규칙은 `tools/logo/README.md`.
      → 적응형 아이콘은 동그라미·둥근 네모·네모·물방울 네 가지로 깎아 보고 확인했다.
      → **기기에서 아이콘이 어떻게 보이는지는 아직 확인하지 않았다.** 앱은 여러 번
        깔아 봤지만 홈 화면·앱 서랍에서 `잠김` 이 읽히는지를 따로 본 적이 없다.
      → 스토어 대표 그래픽은 자물쇠와 `잠` 글자가 **겹쳐 있던 것을 고쳤다** (2026-09-06).
        자리를 비율로 박아 두고 자물쇠가 실제로 얼마나 넓은지 재지 않은 탓이었다.
        이제 자물쇠를 그린 뒤 넓이를 재고 그 옆에 글자를 놓는다.
- [x] 개인정보 처리방침, 스토어 데이터 안전 섹션 작성
      → `docs/개인정보처리방침.md`, `docs/스토어-데이터-안전.md`
        (문의 이메일 주소는 `jamgim.app@gmail.com` 으로 채웠다 — 2026-09-12)
- [x] 스토어 설명에 "절대 안전", "해킹 불가" 류 문구 없음
      → 금지 문구와 쓸 수 있는 문구를 `docs/스토어-데이터-안전.md` 에 정해 두었다.

## 착수 전 확인 사항 (명세 2장) — 확인함

명세는 라이브러리의 **최신 버전과 API 를 직접 확인하고 고정하라**고 했다.
처음에는 이것을 건너뛰고 기억에 있는 버전(SDK 51)을 썼는데, 실기기 빌드를
돌리는 단계에서 그 대가를 치렀다. 지금은 설치된 파일의 타입 정의를 직접 열어
확인했다.

| 라이브러리 | 버전 | 확인한 것 |
|---|---|---|
| `expo` | 57.0.18 (당시 최신) | SDK 51 → 57. `expo-file-system` 이 `File`/`Directory`/`Paths` 로 완전히 바뀌어 저장소 어댑터를 다시 썼다 |
| `react-native` | 0.86.3 | SDK 57 이 요구하는 버전 |
| `react-native-quick-crypto` | 1.1.7 | `createCipheriv`(`authTagLength` 옵션 있음), `createDecipheriv`, `pbkdf2`, `scrypt`, `randomBytes`, **`argon2`** 모두 있음. 입력은 `Buffer` 로 넘겨야 한다 |
| `expo-secure-store` | 57.0.2 | `requireAuthentication` 옵션 유지 |
| `expo-sqlite` | 57.0.2 | 지금은 필드 단위 암호문 + 일반 SQLite. SQLCipher 가 필요하면 그때 바꾼다 |

### Argon2 라이브러리를 없앴다

`react-native-quick-crypto` 1.x 에 Argon2 가 들어 있다. 따로 쓰던
`react-native-argon2` 를 뺐다. 네이티브 모듈이 하나 줄면 빌드에서 깨질 곳도,
공격 표면도 그만큼 준다. AES-GCM · Argon2id · scrypt · PBKDF2 를 모두 한
라이브러리에서 가져온다.

기기에서 Argon2 를 쓸 수 있는지는 앱을 켤 때 아주 작은 값으로 한 번 확인하고,
안 되면 scrypt → PBKDF2 로 내려간다. **어느 경우에도 PBKDF2 60만 회 아래로는
내려가지 않는다** (`assertParamsAcceptable` 이 막는다).

### nitro 버전은 정확히 맞춰야 한다

`react-native-quick-crypto` 는 `react-native-nitro-modules` 위에 올라가 있고,
빌드 때 C++ 코드를 직접 컴파일한다. 이 생성 코드는 **만들어질 당시의 nitro 버전**
헤더를 그대로 참조한다. 버전이 어긋나면 이렇게 깨진다.

    error: NitroModules cannot be found! Are you sure you installed NitroModules properly?
    error: use of undeclared identifier 'PropNameIDCache'

quick-crypto 1.1.7 은 nitrogen 0.33.2 로 만들어졌다(그 패키지의 devDependencies 에
적혀 있다). 그런데 peerDependencies 는 `>=0.31.2` 라고만 해 두어서, 범위를 느슨하게
잡으면 npm 이 0.31.x 를 고른다. 0.31.x 에는 `PropNameIDCache.hpp` 가 아예 없다.

그래서 **정확한 버전으로 고정한다**: `"react-native-nitro-modules": "0.33.2"`.
quick-crypto 를 올릴 때는 그 패키지의 `devDependencies.nitrogen` 을 보고 이 값도
같이 맞춰야 한다. 범위(`^`, `~`)로 두지 않는다.

### 아직 기기에서만 확인 가능한 것

- 위 함수들이 실제 기기에서 값을 제대로 내놓는지 (타입은 맞췄지만 실행은 별개다)
- 생체정보 재등록 시 키 저장소 항목이 무효화되는지
- 파라미터 보정이 실제로 0.5~1초에 맞는지
